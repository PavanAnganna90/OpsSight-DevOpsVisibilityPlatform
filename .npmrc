# Use legacy peer deps to resolve dependency conflicts
# This allows npm to install packages even when peer dependencies don't match exactly
legacy-peer-deps=true

# Ignore optional platform-specific dependencies (like fsevents on Linux)
optional=false

