{
  "buildCommand": "cd frontend && npm install --no-optional && npm run build",
  "installCommand": "cd frontend && npm install --no-optional",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs",
  "rootDirectory": "frontend"
}
