# Use legacy peer deps to resolve dependency conflicts
legacy-peer-deps=true

# Omit optional dependencies to avoid platform-specific package issues
omit=optional

# Force installation to bypass platform checks (for lightningcss platform-specific binaries)
# Note: We allow scripts to run so lightningcss can install the correct Linux binary
force=true

# Prevent hoisting for native modules - they must be in frontend/node_modules
# This ensures Next.js can find the native binaries at build time
public-hoist-pattern[]=*lightningcss*
public-hoist-pattern[]=*@tailwindcss/oxide*

